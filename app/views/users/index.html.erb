<div class="container friends-container">

  <h2 class="bungee-font-m">Friends</h2>
  <div class="friends-search">
      <%= form_tag users_path, method: :get, class: "form-inline search-bar-index" do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Find a friend"
        %>
      <%= submit_tag "Search", class: "btn btn-dark" %>
    <% end %>
  </div>

  <% @users.each do |f| %>
    <div>
      <div class="card-friend">
        <%= cl_image_tag(f.photo.key, width: 400, height: 300, crop: :fill) %>
        <div class="card-friend-infos">
          <h2>Friend name: <%= f.username %> </h2>
          <p><%= f.email%> </p>
        </div>
      </div>
    </div>
  <% end %>

</div>
