button = document.querySelector(".fs-it-btn");
if (button) {
     const text = `<i class="fas fa-check"></i>
    Accepted!`
    button.innerHTML=text;
    button.disabled = true;
    friendlist = document.querySelector("#friendslist")
    if(friendlist){
        friendlist.innerHTML = "<%= j render(partial: 'pages/friends', :locals => {user: current_user}) %>";
    }
}
